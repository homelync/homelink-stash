
export enum NotificationRecipientType {
    /**
    * {@link HomelinkNotification | 'Notification'} recipient is a resident.
    */
    RESIDENT = 'RESIDENT',
     /**
    * {@link HomelinkNotification | 'Notification'} recipient is a landlord user.
    */
    LANDLORD = 'LANDLORD',
     /**
    * {@link HomelinkNotification | 'Notification'} recipient is an installer.
    */
    INSTALLER = 'INSTALLER',
     /**
    * {@link HomelinkNotification | 'Notification'} recipient is a landlord contact.
    */
    CONTACT = 'CONTACT'
}